(library
 (name proof_assistant)
 (public_name proof-assistant)
 (libraries base stdlib)
 (modules logic peano ))
;  logic_parser logic_lexer)) 

; (ocamllex
;  (modules logic_lexer))

; (menhir
;  (modules logic_parser))

(env
  (dev (flags (:standard -warn-error -32-33))))

